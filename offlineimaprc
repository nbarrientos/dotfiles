[general]
# NOTE: cronjob calls the quiet UI with -u
ui = Blinkenlights
accounts = CERN
maxsyncaccounts = 1

[Account CERN]
localrepository = CERN-Local
remoterepository = CERN-Remote
autorefresh = 5

[Repository CERN-Local]
type = Maildir
localfolders = ~/mail/cern

[Repository CERN-Remote]
type = IMAP
ssl = yes
auth_mechanisms = GSSAPI
sslcacertfile = /etc/ssl/certs/ca-certificates.crt
remotehost = imap.cern.ch
remoteuser = ibarrien
folderfilter = lambda folder: folder in ['ai-admins', 'ai-config-team', 'ai-discuss', 'ai-jira', 'ai-jira-crm', 'building-31', 'c5-minutes', 'cern-personnel', 'Drafts', 'foreman-announce', 'foreman-users', 'gitlab', 'i3-discuss', 'INBOX', 'it-dep', 'it-dep-cm', 'it-dep-cm-lcs', 'linux-users', 'oss-security', 'puppet-dev', 'puppet-users', 'rh-errata', 'Sent Items', 'service-desk', 'service-desk-crap', 'spanish-gang', 'Trash']
